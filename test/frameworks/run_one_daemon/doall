#! /bin/bash

# PROJECT_DIR
PROJECT_DIR=`( cd ../../../..; pwd )`
export PROJECT_DIR

if [ "$1" == "start" ] ; then
    ./rundaemon &
    echo daemon started
else
    kill `cat dpid`
    echo daemon stopped
fi
